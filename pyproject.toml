[project]
name = "medicine-box"
version = "0.1.0"

[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["db*", "api*", "ml*", "mqtt*"]
